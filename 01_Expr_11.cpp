#include<bits/stdc++.h>
using namespace std;

size_t fac(size_t n) {
    return (n == 1 || n == 0) ? 1 : n * fac(n - 1);
}


int main()
{
    double ans = (  (M_PI  - (fac(10)/pow(8,8)) + ( pow(log(9.7),( (7/(sqrt(71)) - sin(M_PI*40/180)))     )) ) ) / (pow(1.2,pow(2.3,1/3.)) )   ;
    cout << ans;
}   